import { makeStyles } from '@mui/styles';

export default makeStyles({
  root: {
    marginTop: 40,
    marginBottom: 40,
  },
  cardsContainer: {
    display: 'flex',
    alignItems: 'center',
  },
});
