import { makeStyles } from '@mui/styles';

export default makeStyles({
  root: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 100,
  },
  drawButton: {
    marginTop: 60,
  },
  fightButton: {
    marginTop: 30,
  },
});
